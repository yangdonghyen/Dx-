# Project Handoff

## Current Goal

- React 19, TypeScript, Vite, Tailwind CSS 기반의 모바일 우선 여행 계획 서비스를 완성하고, 일반·시니어 사용자의 핵심 흐름을 접근성 높게 다듬는다.

## Completed

- 로그인, 홈, 텍스트·음성 검색, 여행 생성, 전체·오늘 일정, 저장·지난 여행, 알림, 마이 화면의 주요 흐름 구현.
- 시니어 모드 기본 활성화, 홈의 핵심 메뉴 4개 고정 배치, 알림 진입점과 오늘의 여행 하단 내비게이션 적용.
- 날씨별 계획 명칭을 계획 1/2/3으로 통일하고 계획 2의 비 애니메이션과 일정 적용 흐름 구현.
- 오늘의 여행 지도에 일정 번호·경로를 표시하고 휴식 장소 삽입 시 이후 일정을 30분씩 조정하도록 구현.
- 일반·위성 지도, 위치 권한 실패 시 기본 위치 대체, 지도 출처 표시 적용.
- Obsidian 볼트의 `Projects/figma-make-app` 정션으로 실제 작업 폴더를 연결.
- GitHub `origin/main`의 최신 변경(`291e630`)을 로컬 작업과 3-way 병합.
- 병합 후 production build와 일정·편의성 테스트 17개 통과.
- 오늘의 여행에서 계획 2의 비 애니메이션을 제거하고 제목부터 일정 목록 바탕까지 이어지는 회색 그라데이션 적용.
- 홈 알림 버튼의 흰색 박스를 제거하고 종 아이콘을 약 30% 확대하되 48px 터치 영역은 유지.
- 저장 화면의 AI 여행 취향 분석과 지난 여행 보기를 동일한 크기의 좌우 정사각 카드로 구성.

## In Progress

- 병합 결과의 실제 화면 반응형·접근성 검증.

## Next

- 360px·390px·1024px 및 작은 화면 높이에서 홈, 오늘의 여행, 저장 화면 검증.
- 모든 주요 터치 영역 44px 이상, 키보드 포커스, 스크린리더 이름 점검.
- `App.tsx`를 화면, 공통 UI, 상태, 지도·서비스 로직 단위로 점진적으로 분리.
- 인증, 날씨, AI 일정, 검색, 지도 경로 API 계약과 오류 상태 정의.

## Key Decisions

- 홈은 시니어 모드 전환 바와 카드 페이지 이동 없이 핵심 메뉴 4개를 고정 표시함.
- 시니어 모드는 마이 화면에서 변경하며 기본값은 활성화 상태임.
- 날씨 일정은 계획 1/2/3 명칭을 사용하고 계획 2에는 모션 없이 회색 그라데이션 배경만 표시함.
- 모든 로딩·모달·일정은 모바일 앱 셸 안에 표시하고 DAY 목록은 가로 탐색을 사용함.
- 지난 여행은 읽기 전용 별도 상태로 유지하고 현재 일정과 분리함.
- Obsidian 연결은 저장소에 `.obsidian`을 추가하지 않고 기존 볼트의 디렉터리 정션을 사용함.
- 실제 작업 폴더와 Git 미러(`C:\Users\smhrd\Desktop\프로젝트\Dx-incoming-20260921`)는 분리되어 있어 동기화가 필요함.

## Known Issues

- 약 2,000줄 규모의 `App.tsx`로 인한 유지보수·회귀 위험.
- 인증, 날씨, 검색, 지도 경로, AI 일정 등 외부 연동이 대부분 목 데이터 기반임.
- 위치정보 동의·보관·철회와 지도 공급자 운영 정책이 미확정임.
- 실제 기기의 접근성·반응형 검증이 남아 있음.
- 작업 폴더와 Git 미러 분리로 동기화 누락 위험이 있음.

## Important Files

- `src/App.tsx`
- `src/index.css`
- `src/main.tsx`
- `src/lib/itinerary.ts`
- `src/lib/comfort.ts`
- `tests/itinerary.test.mjs`
- `tests/comfort.test.mjs`
- `package.json`
- `vite.config.ts`
