# 여행메이트 핸드오프

최종 업데이트: 2026-09-22  
기준 커밋: `d1c22f6` (`main`, GitHub 업로드 완료)  
프로젝트: React 19 + TypeScript + Vite + Tailwind CSS 4 모바일 우선 목업

## 현재 상태

- 초기 진입은 로그인 화면이며, 로그인 후 설정을 거쳐 홈으로 진입한다.
- 기본값은 시니어 모드 활성화(`seniorMode: true`)다. 시니어 모드 변경은 마이페이지에서 가능하다.
- 앱의 주요 상태와 화면은 `src/App.tsx`에서 관리한다. 서버·인증·AI·검색·지도 경로 데이터는 현재 목업 또는 클라이언트 상태 기반이다.
- GitHub 원격 저장소 `origin/main`과 동기화됐으며, 마지막 업로드는 `d1c22f6`이다.

## 이번 세션 반영 사항

### 홈·내비게이션

- 시니어 홈의 기능 카드 페이지 이동(이전/다음)을 제거했다.
- 홈에는 아래 4개 핵심 메뉴만 고정 표시한다.
  - 전체 일정
  - 오늘의 일정
  - 여행 만들기
  - 오늘 날씨
- 홈에서 저장한 장소·지도 탐색·내 설정 카드를 제거했다.
- 홈을 고정 높이 레이아웃으로 바꿔 스크롤 없이 검색 영역과 4개 카드를 화면에 배치했다.
- 홈 우측 상단 알림 아이콘을 복원했다. 읽지 않은 알림은 빨간 점으로 표시되며, 누르면 알림 화면으로 이동한다.
- 홈 상단의 시니어 모드 전환 바는 제거했다.
- 오늘의 여행 화면에도 하단 `홈 / 탐색 / 저장 / 마이` 내비게이션을 표시하며, 이 화면에서는 홈 탭을 활성화 상태로 표시한다.

### 저장·지난 여행·마이페이지

- 지난 여행 진입점을 홈 카드에서 제거했다.
- 하단 `저장` 화면에 `지난 여행 보기` 버튼을 추가해 지난 여행 목록으로 이동할 수 있다.
- 마이페이지 메뉴 카드의 보조 설명을 제거했다. 메뉴 아이콘과 이름, 기능은 유지한다.
- 마이페이지 메뉴는 하나의 위젯 안에 아래 6개 카드로 구성된다.
  - 내 여행 일정 / 지난 여행 / 저장한 장소
  - 여행 취향 / 시니어 모드 / AI 취향 재분석

### 오늘 날씨·오늘의 여행

- 날씨 일정 표기를 `계획 1 / 계획 2 / 계획 3`으로 통일했다.
  - 계획 1: 기본 일정
  - 계획 2: 비 오는 날 일정
  - 계획 3: 더운 날 일정
- 계획 적용 후 오늘의 여행으로 이동하며 해당 일정에 `계획 1/2/3` 태그가 반영된다.
- 계획 2에서만 오늘의 여행 지도와 일정 영역에 비 애니메이션이 표시된다.
- 계획 1과 계획 3의 햇살·후광 애니메이션은 사용자 요청에 따라 제거했다.
- 오늘의 여행에서 길찾기 버튼과 상세보기 내부의 장소 길찾기 버튼은 제거했다.
- 오늘의 여행은 Leaflet 지도에 일정 순서 번호·경로를 표시한다.
- 휴식하기 흐름은 유지된다. 일정 선택 → 휴식 장소 탐색 → 추천 휴식지 삽입 시, 이후 일정 시간을 30분씩 늦춘다.

### 지도

- 탐색 지도는 React Leaflet + OpenStreetMap을 사용한다.
- 브라우저 위치 권한을 요청하며, 실패 시 `광주광역시 남구 송하동 60` 좌표(`35.1102272, 126.8819292`)를 기본값으로 사용한다.
- 일반 지도와 위성 지도(Esri World Imagery) 전환을 지원한다.
- 오늘의 여행 지도는 일정별 번호 마커와 Polyline 경로를 표시한다.

## 검증 현황

- 최근 production build 통과: `corepack pnpm run build`
- 최근 커밋 및 GitHub 업로드 완료: `d1c22f6`
- 일정 편의성 로직 테스트 파일: `tests/comfort.test.mjs`
- 시각·반응형 브라우저 검증은 별도 수행이 필요하다.

## 현재 주의 사항 / 기술 부채

| 우선순위 | 항목 | 현황 및 권장 조치 |
| --- | --- | --- |
| P1 | 시니어 접근성 | 기본 확대는 110% 중심이다. 글자 크기 단계, 고대비, 앱 내부 모션 줄이기 옵션을 추가하는 것이 좋다. |
| P1 | 터치 영역 | 주요 CTA는 크지만 뒤로가기·닫기·태그·일부 보조 버튼은 44px 미만일 수 있다. 전 화면 터치 영역을 점검한다. |
| P1 | 데이터 영속성 | 로그인, 시니어 설정, 여행 취향, 일정, 저장 장소는 서버/로컬 저장소에 영속화되지 않는다. 새로고침·재접속 시 유지 정책이 필요하다. |
| P1 | 외부 연동 | 로그인·카카오·네이버, AI 일정, 검색, 지도 경로, 날씨·행사 정보는 실제 API가 아닌 목업 또는 UI 흐름이다. |
| P1 | 위치정보 | 기본 좌표는 임시 검증값이다. 운영 전 지도 공급자, 위치 동의·보관·철회 정책, 정밀 지오코딩을 확정해야 한다. |
| P2 | 단일 대형 파일 | 화면과 상태가 `src/App.tsx`에 집중돼 있다. 화면별 컴포넌트와 상태/도메인 로직 분리가 필요하다. |
| P2 | 사용하지 않는 스타일 | 반복적인 날씨 애니메이션 실험 후 남은 CSS가 있을 수 있다. 런타임에 사용하지 않는 햇살 관련 클래스를 정리한다. |
| P2 | 시각 검증 | 고정 모바일 프레임 외 360px, 390px, 작은 높이, 키보드·스크린리더 흐름을 실제 브라우저에서 점검해야 한다. |

## 다음 작업 권장 순서

1. 시니어 접근성 강화: 글자 크기 단계, 고대비, 모션 줄이기, 모든 버튼 44px 이상 점검.
2. 오늘의 여행 실기기 검증: 하단 내비게이션이 추가된 뒤 지도·일정 카드·휴식 패널의 스크롤과 가림 확인.
3. 저장 화면 정보 구조 검증: 저장 장소와 지난 여행의 분리/탐색 흐름이 사용자 기대와 맞는지 확인.
4. 인증·날씨·지도·경로·AI 일정의 실제 API 계약 및 오류 상태 정의.
5. `App.tsx`를 화면·공통 UI·상태·지도 로직 단위로 분리하고, 미사용 날씨 CSS를 정리.
6. 보호자 공유·도움 요청 등 시니어 특화 안전 기능의 제품 정책과 개인정보 동의 기준 결정.

## 주요 파일

- `src/App.tsx`: 화면 전환, 앱 상태, 로그인, 홈, 일정, 지도, 날씨, 프로필 구현
- `src/index.css`: 전역 토큰, 시니어 확대, 날씨 애니메이션 스타일
- `src/lib/itinerary.ts`: 여행 일정 생성 로직
- `src/lib/comfort.ts`: 일정 불편 사유 기반 완화 로직
- `tests/comfort.test.mjs`: 일정 완화 로직 테스트
- `DESIGN_SPEC.md`: 초기 디자인 명세 참고 문서
- `Handoff.md`: 본 인수인계 문서