# 디자인·프론트엔드 Handoff

작성일: 2026-09-21  
대상: 여행메이트 모바일 우선 여행 계획·현장 안내 목업

## 현재 상태

- React 19 + TypeScript + Vite + Tailwind CSS 4 단일 페이지 목업이다.
- 의존성 설치와 production build가 완료됐고 개발 서버는 `http://localhost:8443`에서 확인됐다.
- 로그인, 취향 설정, 장소 탐색, 여행 생성, AI 일정, 숙소, 당일 여행, 길찾기, 프로필 데모 흐름이 `src/App.tsx` 상태 기반으로 동작한다.
- 인증·검색·지도·경로·AI·저장 API는 아직 mock 데이터다.

## 현재 애로사항

| 우선순위 | 문제 | 사용자 영향 | 조치 |
| --- | --- | --- | --- |
| P0 | 하단 CTA가 `fixed`여서 모바일 목업 컨테이너가 아닌 브라우저 뷰포트에 배치된다. | 데스크톱 미리보기에서 CTA가 프레임 밖으로 넓어지거나 콘텐츠를 가린다. | `header / scrollable content / action bar` 구조로 바꾸고 CTA를 shell 내부 `sticky` 영역으로 둔다. (`App.tsx`: 657, 756, 1017, 1178, 1561) |
| P1 | 알림·프로필·뒤로·닫기 같은 아이콘 버튼에 접근 가능한 이름이 없다. | 스크린리더·음성 제어 사용자가 기능을 구별하기 어렵다. | 문맥별 `aria-label`과 통일된 `:focus-visible`을 추가한다. |
| P1 | 바텀시트에 dialog 의미, 포커스 트랩/복귀, Escape 처리가 없다. | 키보드 포커스가 배경으로 새고 보조기술 맥락이 불명확하다. | `role=dialog`, `aria-modal`, 제목 연결, 포커스 관리, Escape 닫기를 구현한다. (`App.tsx`: 1329) |
| P1 | 검색 UI가 `readOnly input`이며 클릭 시 음성 검색으로 이동한다. | 텍스트 검색을 기대한 사용자가 오해한다. | 실제 검색 필드와 음성 액션을 분리하거나 ‘음성으로 장소 찾기’ 버튼으로 바꾼다. (`App.tsx`: 304) |
| P1 | 384×844 목업에 고정되어 실제 웹의 반응형 규칙이 없다. | 태블릿·데스크톱에서 정보 밀도와 CTA 위치가 불안정하다. | 목업 전용/반응형 웹 중 플랫폼을 확정하고 360·768·1024px 규칙을 적용한다. |
| P2 | 색상·그림자·radius가 inline 값으로 반복된다. | 유지보수와 상태 일관성이 떨어진다. | semantic token과 공통 컴포넌트로 승격한다. |
| P2 | 시니어 모드는 글자 크기만 110%로 확장한다. | 터치 영역·행간·정보 밀도가 개선되지 않는다. | typography, spacing, target size, 대비를 함께 바꾸는 density 규칙을 만든다. |

## 다음 할 일

1. P0: `AppShell` 내부에 ActionBar/BottomNav를 배치해 CTA 가림을 제거한다.
2. P1: 아이콘 버튼 이름, focus-visible, 모달 키보드 동작, 선택 상태 ARIA를 적용한다.
3. P1: 텍스트·음성·사진 검색의 진입점과 로딩/권한거부/빈 결과/오류를 확정한다.
4. P2: `AppShell`, `PageHeader`, `PrimaryButton`, `SelectionCard`, `ActionBar`, `BottomSheet`를 공통화한다.
5. 플랫폼 결정을 바탕으로 반응형 규칙을 구현한다.
6. 인증·검색·일정·지도·AI API의 데이터 및 실패 상태 계약을 정의한다.
7. ‘여행 만들기’와 ‘당일 길찾기’ 과업으로 사용성 검증을 수행한다.

## 완료 기준

- 360px과 1024px 이상에서 CTA/BottomNav가 콘텐츠를 덮지 않는다.
- 모든 인터랙티브 요소는 키보드로 조작 가능하고 이름·포커스가 명확하다.
- 모달은 포커스를 관리하고 닫힌 뒤 호출 요소로 복귀한다.
- 여행 생성의 선택 전/후, 유효하지 않음, 로딩, 오류 상태가 정의된다.
- production build와 변경 흐름의 브라우저 검증이 통과한다.

## 결정 필요

- 모바일 앱 목업인지 반응형 웹 서비스인지
- 일반 여행자와 시니어/가족 사용자 중 우선 타깃
- AI 추천의 근거, 수정 가능 범위, 실패 시 대안
- 지도·경로·소셜 로그인 공급자와 개인정보 동의 방식

## 관련 문서

- `DESIGN_SPEC.md`: 디자인 시스템과 화면/상태 명세
- `src/App.tsx`: 현재 목업 구현
- `src/index.css`: 현재 전역 토큰과 스타일

## 업무 우선순위 재정렬 (2026-09-21)

| 순위 | 업무 | 이유 | 완료 산출물 |
| --- | --- | --- | --- |
| P0-A | AppShell·CTA·BottomNav 레이아웃 정리 | 모든 화면의 가림과 반응형 문제를 해결하는 기반이다. | shell 구조, 360/390/1024px 검증 결과 |
| P0-B | 시니어 전용 ‘편안한 여행’ 페이지 기획·설계 | 시니어 편의는 글자 확대 옵션만으로 충족되지 않으며, 당일 핵심 과업에 직접 영향을 준다. | 목적/흐름/와이어 기준/상태/접근성 명세 |
| P1-A | 시니어 전용 페이지 구현 | P0-B 기획을 실제 사용자 흐름으로 검증한다. | 화면, 진입점, mock 상태, 키보드 검증 |
| P1-B | 접근성 기반 적용 | 아이콘 이름, focus, 모달, 선택 상태는 전 화면 공통 품질 기준이다. | 공통 컴포넌트와 키보드 테스트 |
| P1-C | 검색 진입점 재설계 | 텍스트·음성·사진 검색의 기대 불일치를 제거한다. | 세 검색 흐름과 오류/권한 상태 |
| P2 | 디자인 시스템 공통화와 API 계약 | 반복 구현을 줄이고 실데이터 전환을 준비한다. | 토큰/컴포넌트/API 상태 계약 |
| P3 | 사용성 검증과 개선 | 실제 시니어·동행자 과업 검증으로 가설을 보정한다. | 과업 결과와 개선 백로그 |

### 시니어 전용 페이지의 제품 결정

- 페이지명은 **편안한 여행**으로 한다. 단순 설정 페이지가 아니라 여행 당일 빠른 실행을 위한 독립 화면이다.
- 진입점은 홈의 현재/다가오는 여행 카드와 프로필의 시니어 모드 설정 안에 둔다. BottomNav의 기존 정보 구조는 유지한다.
- 초기 범위는 다음 행동, 오늘 일정, 길찾기, 도움 요청, 여행 설정 네 가지로 제한한다. 가족 공유·건강 정보·긴급 연락은 개인정보/동의 정책이 확정되기 전에는 설계 후보로만 둔다.
- 시니어 사용자는 기능이 축소된 사용자가 아니라, 더 명확한 정보 밀도와 더 큰 조작 여유가 필요한 사용자로 정의한다.

## 구현 진행 현황 (2026-09-21)

- [x] P0-A 1차: 화면 내부 CTA의 뷰포트 `fixed` 배치를 제거하고 화면 flex 레이아웃 안의 하단 영역으로 전환했다.
- [x] P0-A 1차: 앱 프레임 높이를 `min(844px, 100dvh)`로 바꿔 작은 높이의 viewport에서 프레임 초과를 줄였다.
- [x] P0-B: 홈에서 진입 가능한 `편안한 여행` 전용 화면을 추가했다.
- [x] P0-B: 전용 화면에 다음 행동, 길찾기, 오늘 일정, 빠른 도움, 화면 설정 진입을 구현했다.
- [x] 공통 개선: `PageHeader`의 뒤로가기 버튼에 접근 가능한 이름을 추가했다.
- [x] 검증: production build 통과, 개발 서버 HTTP 200 응답 확인.
- [ ] 다음 검증: 360/390/1024px의 실제 화면 가림·스크롤·탭 순서 확인.
- [ ] 다음 구현: 아이콘 버튼 전체의 이름·focus-visible, 바텀시트의 focus trap/Escape, 검색 진입점 재설계.

## 지도 기능 구현 (2026-09-21)

- OpenStreetMap 타일과 React Leaflet 기반의 실제 지도를 `지도 탐색` 화면에 연결했다.
- 페이지 진입 시 브라우저 Geolocation API로 GPS 위치를 요청한다. 권한 거부, 시간 초과, 미지원 환경에서는 자동으로 기본 위치를 쓴다.
- 기본 위치: `광주광역시 남구 송하동 60`, `35.1102272, 126.8819292`. 공개 지오코더가 주소를 송하동 중심으로 반환한 값이므로, 운영 전 도로명 단위의 상용 지오코딩으로 재검증한다.
- 지도에는 현재 위치, 카테고리별 주변 추천 마커, 현재 위치 재시도, GPS/기본 위치 상태 문구를 제공한다.
- 추가된 의존성: `leaflet`, `react-leaflet`, `@types/leaflet`.
- 검증: production build 통과 및 Vite 서버 HTTP 200 확인.
- 운영 전 확인: OpenStreetMap 타일 사용 정책·트래픽 한도, 지도/지오코더 공급자, 위치정보 동의·보관·철회 정책을 확정한다.

## 지도 후속 작업과 위성지도 (2026-09-21)

- [x] 지도 유형 토글: 일반 지도(OpenStreetMap)와 위성 지도(Esri World Imagery)를 전환할 수 있다.
- [x] 지도 출처 표기: 두 타일 레이어의 attribution을 지도에 표시한다.
- [ ] **추후 작업**: 송하동 기본 위치를 도로명 주소 단위로 정밀 지오코딩해 재검증한다.
- [ ] **추후 작업**: 운영 트래픽에 적합한 지도/위성 타일 공급자, API 키, 사용량 한도와 비용을 확정한다.
- [ ] **추후 작업**: 위치 권한 동의, 철회, 위치 데이터 보관 정책을 제품·법무 기준으로 확정한다.

## 시니어 홈 화면 개선 (2026-09-21)

- [x] 시니어 모드에서만 별도 홈 화면을 표시하도록 분기했다.
- [x] 넓은 검색 진입 영역, 파스텔 배경, 큰 원형 기능 아이콘, 2×2 기능 그리드, 페이지 번호와 이전/다음 이동을 적용했다.
- [x] 첫 페이지: 편안한 여행, 여행 만들기, 지도 탐색, 저장한 장소.
- [x] 두 번째 페이지: 오늘의 일정, 전체 일정, 지난 여행, 내 설정.
- [x] 모든 기능 카드는 큰 터치 영역과 라벨을 갖고 기존 화면으로 연결된다.
- [x] production build 통과.

## 시니어 앱 기본 활성화 (2026-09-21)

- [x] 목업의 초기 화면을 로그인 화면에서 시니어 홈 화면으로 변경했다.
- [x] 초기 `seniorMode`를 `true`로 설정했다.
- [x] 초기 설정 화면에서도 시니어 모드를 기본 선택으로 변경했다.
- 일반 모드는 삭제하지 않았으며, 설정/프로필에서 필요 시 전환할 수 있다.
- [x] production build 통과.
